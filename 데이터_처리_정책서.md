# 두두 로지스틱스 - 데이터 처리 정책서

- **작성자**: 유현재
- **작성일**: 2026-08-06
- **대상**: 접수 원장 `shipments` 3,045건 / 25칸

---

# 오늘 내는 것 세 가지
 - 데이터 비교 및 처리 방안 정리

## 0. 이번 비교 결과(실측)

- 두 파일은 모두 3,045건이고, `tracking_no` 기준으로 순서도 동일해서 **행 추가/삭제 없이 값 보정과 파생 컬럼 추가**로 판단했습니다.
- 원본 파일의 컬럼 수는 25개였고, 신규 파일은 31개로 늘었습니다. 추가된 파생 컬럼은 아래 6개입니다.
  - `lead_time_hours`
  - `lead_time_days`
  - `lead_time_is_negative`
  - `lead_time_issue_reason`
  - `lead_time_hours_clean`
  - `lead_time_days_clean`
- 공통 컬럼 중 값 차이가 확인된 항목은 총 8개였습니다.
  - `accepted_at`: 181건
  - `receiver_phone`: 2,600건
  - `weight_kg`: 82건
  - `width_cm`: 3,045건
  - `billed_weight_kg`: 3건
  - `price`: 299건
  - `eta_date`: 161건
  - `delivered_at`: 65건
- 실제로는 **형식 정규화**, **결측값 보정**, **파생값 추가**가 동시에 나타났고, 특히 `accepted_at`, `eta_date`, `delivered_at`, `price`는 보정/정규화가 중심이었습니다.

## 1. 정리 방식 — 어떤 기준으로 처리했는가

### 1). 비교 키(정합 기준)
- `tracking_no` 기준으로 두 파일을 **1:1 매칭**하여 비교했습니다.
- 전체 행 수는 둘 다 **3045행**이며, `tracking_no`도 전 행 **동일한 순서/값**이라  
  “행 추가/삭제” 이슈가 아니라 **값 보정/정규화 및 파생 컬럼 추가** 성격으로 판단했습니다.

### 2). 스키마(컬럼) 비교
- `shipments_origin.csv`: **25 컬럼**
- `shipments.csv`: **31 컬럼**
- 즉, **신규 컬럼 6개**가 추가되었는지부터 확인했습니다.

### 3). 값 변경 비교 범위/방법
- 공통 컬럼(기존 25개)에 대해 전체를 다 보기보다,  
  “값이 실제로 달라진 컬럼”만 **diff 카운트**로 추려 확인했습니다.
- 값이 바뀐 공통 컬럼은 **8개**였습니다.
  - `accepted_at` (181건)
  - `receiver_phone` (2,600건)
  - `weight_kg` (82건)
  - `width_cm` (3,045건)
  - `billed_weight_kg` (3건)
  - `price` (299건)
  - `eta_date` (161건)
  - `delivered_at` (65건)
- 이 중 `accepted_at`, `eta_date`, `delivered_at`은 날짜/시간 포맷 정규화와 결측 보정이 섞여 있었고,
  `receiver_phone`는 하이픈/공백 제거 같은 표현 정규화가 우세했습니다.

### 4). 변경 성격 분류 기준
변경은 아래 3가지로 분류했습니다.

- (A) **표현/포맷 정규화**  
  예: 문자열 날짜/시간 → 표준 날짜/시간 형식
- (B) **결측치 보정**  
  예: `NaN` → 특정 값으로 채움
- (C) **파생값 추가**  
  예: 리드타임(lead time) 계산 컬럼 추가

---

## 2. 오류 분류 — 어떤 예외가 나왔는가

### 1). 날짜/시간 포맷 혼재(정규화 대상)
원본의 `accepted_at`는 아래처럼 여러 포맷이 혼재되어 있었습니다.

- `2026/05/21 15:47`
- `2026.06.12 16시21분`
- `26-05-19 16:00`
- `05/25/2026 14:04`

신규 파일에서는 이를 `YYYY-MM-DD HH:MM:SS`로 일괄 표준화하여,  
diff 상으로는 “값이 바뀐 것처럼” 보이지만 실제 의미는 동일한 케이스가 많습니다.

`eta_date`도 아래처럼 혼재 → 표준화되었습니다.

- `2026.06.25`, `2026/06/24` 등 → `YYYY-MM-DD`

### 2). 결측치(NaN) 보정 발생
- `eta_date`  
  - 원본 `NaN`이 신규 파일에서 **날짜로 채워진 케이스** 존재  
  - diff **161건** 중 상당수가 해당 유형으로 추정
- `delivered_at`  
  - 원본 `NaN` → 신규에서 `2026-07-08 18:00:00` 같은 값으로 채워진 케이스 존재  
  - diff **65건**
- `price`  
  - 원본 `NaN` → 신규에서 `4000`, `6000` 등으로 채워진 케이스 다수  
  - diff **299건**

### 3). 수치/문자열 포맷 정규화 이슈
- `receiver_phone`는 `010-8199-7563` 같은 형식이 `01081997563`로 바뀌는 사례가 다수 확인되었습니다.
- `weight_kg`, `width_cm`, `billed_weight_kg`는 `kg` 단위 문자열, 빈 문자열, 소수점 표기 차이로 인해 **형식 정규화/비어 있는 값 처리**가 섞여 있었습니다.
- 이런 값은 “실제 데이터 의미”가 동일해도 문자열 표현이 달라져 diff로는 크게 잡히는 경우가 있습니다.

### 3). 리드타임(lead time) 파생 컬럼 관련 예외
신규 파일에 리드타임 파생 컬럼 **6개** 추가:

- `lead_time_days`
- `lead_time_hours`
- `lead_time_days_clean`
- `lead_time_hours_clean`
- `lead_time_is_negative`
- `lead_time_issue_reason`

관측 사항:

- `lead_time_days` / `lead_time_hours`는 **결측 257건** 존재  
  → `accepted_at` 또는 `delivered_at` 등 계산 입력값 결측 또는 **파싱 실패** 가능성이 큼
- `lead_time_issue_reason`는 대부분 비어있음(거의 전부 `NaN`)  
  → “문제 사유를 남기려는 의도는 있었으나, 아직 로직/기록이 미구현/미적용” 상태로 보임

---

## 3. 처리 방안 — 다음 단계에서 어떻게 할 것인가

### (필수) 1). 변경을 “의도된 정규화” vs “의도되지 않은 값 수정”으로 분리 기록
- `accepted_at`, `eta_date`는 우선 **정규화(포맷 통일)**로 분류합니다.
- 검산/리포트에서는 “문자열 표현만 변경된 것”은 변경으로 오탐하지 않도록  
  룰을 명확히 둡니다.
  - 예: 날짜 파싱 후 동일 시각/동일 날짜면 **“동일”**로 처리

### (필수) 2). 결측 보정 근거 추적(출처/규칙 확인)
`price`, `eta_date`, `delivered_at`의 `NaN` → 값 채움은  
“좋은 보정”일 수도, “잘못 채운 값”일 수도 있어 **보정 출처(규칙/로직/근거 데이터)**가 필수입니다.

#### 다음 단계 작업
- `NaN`이 채워진 행만 **필터링**하여 목록화
- 채워진 값이 다음과 같은 규칙과 일치하는지 **샘플 검증**
  - `status`, `channel`, `branch`
  - 요금 규칙(가격 산출)
  - ETA 산출 규칙
- 보정 규칙을 **문서화**
  - “어떤 조건이면 `NaN`을 채운다/안 채운다”
  - 예외 케이스 처리 기준 포함

### (필수) 3). 리드타임 품질 게이트(Quality Gate) 추가
`lead_time_days/hours` 결측(257건) 및 `lead_time_is_negative`를 활용해  
데이터 품질 체크 규칙을 고정합니다.

#### 권장 품질 규칙
- `accepted_at` 또는 `delivered_at` 결측 시
  - 리드타임 **미계산** 처리
  - `lead_time_issue_reason`에 사유 기록  
    (예: `missing_accepted_at`, `missing_delivered_at`, `parse_fail`)
- 리드타임 **음수** 발생 시
  - 원인(타임존/파싱/입력 오류) 분류
  - 별도 큐로 분리하여 **정정/재처리 대상화**

### (권장) 4). 재현 가능한 “변경 리포트” 자동화
다음에도 동일 비교가 반복될 가능성이 높으므로, 아래 항목을 자동 산출하는 스크립트/노트북을 남깁니다.

- 추가 컬럼 목록
- 기존 컬럼 값 변경 건수 Top
- `NaN` → 값 보정 리스트
- 리드타임 계산 불가/이상치 리스트

## 조사 현황

내가 본 칸에 체크합니다. **안 본 칸도 정보입니다.**

- [ ] `tracking_no` - 운송장 번호
- [ ] `accepted_at` - 접수 시각
- [ ] `branch_code` - 지점 코드
- [ ] `branch_name` - 지점 이름
- [ ] `sender_id` - 보내는 사람 ID
- [ ] `sender_name` - 보내는 사람 이름
- [ ] `receiver_name` - 받는 사람 이름
- [ ] `receiver_phone` - 받는 사람 전화
- [ ] `receiver_area` - 받는 지역
- [ ] `receiver_dong` - 받는 동
- [ ] `region_type` - 권역 구분
- [ ] `category` - 물품 분류
- [ ] `item_name` - 물품명
- [ ] `weight_kg` - 무게
- [ ] `width_cm` - 가로
- [ ] `height_cm` - 세로
- [ ] `depth_cm` - 높이
- [ ] `volume_weight_kg` - 부피 무게
- [ ] `billed_weight_kg` - 청구 무게
- [ ] `size_grade` - 크기 등급
- [ ] `price` - 요금
- [ ] `eta_date` - 도착 예정일
- [ ] `status` - 배송 상태
- [ ] `delivered_at` - 배송 완료 시각
- [ ] `channel` - 접수 경로

---

## 쓰기 전에 - 세 가지 눈금

칸을 채울 때 아래 셋을 골라 적습니다.

### 눈금 1. 처리 분류 - 이 예외를 무엇으로 했는가

- **수정** - 값을 고쳤다
- **삭제** - 행이나 값을 지웠다
- **통일** - 여러 표기를 하나로 묶었다
- **보류** - 손대지 않고 남겼다

### 눈금 2. 어디서 막는가 - 이 예외를 앞으로 어디서 걸러야 하는가

- **기계적 차단** - 화면이 애초에 못 넣게 한다. 필수 입력, 숫자만, 선택 목록
- **정책** - 규칙을 정해야 정해진다. 선택지를 어디까지 열지, 예외를 어디까지 허용할지
- **운영** - 사람이 받아야 한다. 상담원 연결, CS 접수, 환불 처리

세 층을 섞지 않습니다. 정책으로 정할 것을 기계로 막으면 현장이 못 씁니다. 기계로 막을 것을 사람에게 넘기면 같은 일이 계속 들어옵니다.

### 눈금 3. 확신도 - 내가 어디까지 정했는가

- **확정** - 이렇게 합니다
- **추천** - 이게 낫다고 보지만 최종 결정은 팀에서
- **선택지** - 두 갈래를 다 만들어 뒀습니다. 고르시면 됩니다

혼자 확정한 것보다 여럿이 같은 결론에 도달한 것이 믿을 만합니다. 혼자 판단하면 틀리기 쉽습니다.

---

# 칸별 정책

아래 블록을 조사한 칸 수만큼 복사해서 씁니다.

---

## 칸 - `(칸 이름)`

### 1. 초기 상태

- 값 종류:
- 빈 칸:
- 형식: (숫자 / 글자 / 날짜 / 섞임)
- 어떻게 셌나:

### 2. 예외

**예외 1 - (무엇)**

- 몇 건:
- 어떻게 찾았나:

**예외 2 - (무엇)**

- 몇 건:
- 어떻게 찾았나:

### 3. 처리

**예외 1 - (무엇)**

- 분류: (수정 / 삭제 / 통일 / 보류)
- 왜 이렇게 정했나:
- 왜 생겼다고 보는가:
- 확신도: (확정 / 추천 / 선택지)

**예외 2 - (무엇)**

- 분류:
- 왜 이렇게 정했나:
- 왜 생겼다고 보는가:
- 확신도:

### 4. 앞으로 어디서 막나

**예외 1 - (무엇)**

- 층: (기계적 차단 / 정책 / 운영)
- 어떻게:

**예외 2 - (무엇)**

- 층:
- 어떻게:

### 5. 못 정한 것 - 누구에게 물어야 하나

- (없으면 "없음")

---

# 채운 예시 두 개

베껴도 되고 전혀 다르게 써도 됩니다.

---

## 칸 - `price` (요금)

### 1. 초기 상태

- 값 종류: 17가지 (빈 칸 포함)
- 빈 칸: 215건
- 형식: 숫자
- 어떻게 셌나: 필터 메뉴에 `17개 표시 중`. 빈 칸만 골라 화면 오른쪽 아래의 `총 3,045행 중 215행 표시됨`

### 2. 예외

**예외 1 - 빈 칸**

- 몇 건: 215
- 어떻게 찾았나: 필터 > 조건별 필터링 > `(공백)`

**예외 2 - 음수 요금 (`-1000`, `-1500`)**

- 몇 건: 4
- 어떻게 찾았나: 필터 값 목록 맨 위

### 3. 처리

**예외 1 - 빈 칸 215건**

- 분류: 보류
- 왜 이렇게 정했나: 안 받은 돈인지 안 적은 것인지 모른다. 0으로 채우면 못 받은 돈이 받은 것이 된다
- 왜 생겼다고 보는가: 접수 담당자가 나중에 적으려다 잊었을 가능성
- 확신도: 확정

**예외 2 - 음수 4건**

- 분류: 보류
- 왜 이렇게 정했나: 요금이 마이너스일 수는 없지만 지우면 그 건의 흔적이 사라진다
- 왜 생겼다고 보는가: 환불을 요금 칸에 마이너스로 적은 것으로 보임
- 확신도: 추천

### 4. 앞으로 어디서 막나

**예외 1 - 빈 칸**

- 층: 기계적 차단
- 어떻게: 요금은 사람이 입력하지 않는다. 무게와 지역으로 계산해서 자동으로 채운다. 계산이 안 되면 저장을 막는다

**예외 2 - 음수**

- 층: 정책
- 어떻게: 환불을 요금 칸에 적을 것인지 별도 칸을 둘 것인지 정해야 한다

### 5. 못 정한 것 - 누구에게

- 빈 215건이 미수금인지 기록 누락인지 -> 접수 담당자
- 환불 표기 방식 -> 정산 담당

---

## 칸 - `receiver_area` (받는 지역)

### 1. 초기 상태

- 값 종류: 28가지
- 빈 칸: 0건
- 형식: 글자, 자유 입력으로 보임
- 어떻게 셌나: 필터 메뉴 `28개 표시 중`

### 2. 예외

**예외 1 - 같은 지역을 다르게 적음 (`서울` / `서울시` / `서울특별시`)**

- 몇 건: 세어 보지 못함
- 어떻게 찾았나: 필터 값 목록이 가나다순이라 붙어 있음

**예외 2 - 영문 표기 (`SEOUL`)**

- 몇 건: 세어 보지 못함
- 어떻게 찾았나: 목록 끝쪽

### 3. 처리

**예외 1 - 같은 지역 다른 표기**

- 분류: 통일
- 왜 이렇게 정했나: 집계가 안 맞는다. 서울이 세 덩어리로 갈려 있으면 지점별 물량을 못 센다
- 왜 생겼다고 보는가: 자유 입력이라 사람마다 다르게 적었다. 담당자가 바뀌면서 표기도 바뀐 것으로 보임
- 확신도: 확정

**예외 2 - 표준 목록을 무엇으로 할지**

- 분류: 보류
- 왜 이렇게 정했나: 광역시도 17개로 할지 배송 권역으로 할지에 따라 요금이 달라진다
- 왜 생겼다고 보는가: 애초에 정해진 목록이 없었다
- 확신도: 선택지 (둘 다 만들어 뒀습니다)

### 4. 앞으로 어디서 막나

**예외 1 - 표기 갈림**

- 층: 기계적 차단
- 어떻게: 자유 입력을 없애고 선택 목록으로 받는다. 손으로 치면 또 갈린다

**예외 2 - 목록의 범위**

- 층: 정책
- 어떻게: 어느 단위로 나눌지는 요금 규정과 붙어 있어 우리가 못 정한다

### 5. 못 정한 것 - 누구에게

- 표준 목록 단위 -> 현장

---

# 마무리

## 검산

정한 대로 실제로 됐는지 대 봅니다. 맞히려는 것이 아니라 **어디가 다른지 보려는 것**입니다.

**칸 - (이름)**

- 처리 전:
- 처리 후:
- 예상과 달랐던 것:

검산까지 못 갔으면 어디까지 갔는지 적습니다. 그것이 이 칸의 답입니다.

- 어디까지:
- 왜 멈췄나:

## 동료와 대 보기

혼자 내린 판단은 틀리기 쉽습니다. 같은 칸을 본 사람이 있으면 대 봅니다.

**칸 - (이름)**

- 나의 판단:
- 다른 사람 판단:
- 같았나:

다섯 명 중 셋 이상이 같은 결론이면 그건 믿어도 됩니다. 갈리면 그 칸은 보류로 내립니다.

## 원본 보존

- [ ] 원래 칸은 그대로 두고 새 칸을 옆에 만들었다
- [ ] 원래 칸을 덮어썼다

새로 만든 칸:

- (예: `status` 옆에 `status_정리`)
-

원래 칸을 덮어썼다면 되돌릴 방법:

-

## 이번에 다루지 않기로 한 것

안 하기로 정하는 것도 산출물입니다. 하나여도 됩니다.

-

## 다음 사람에게

내일 이 폴더를 여는 사람이 나여도 오늘 무엇을 왜 했는지 기억나지 않습니다. 같은 조사를 두 번 하지 않으려면 무엇을 알아야 합니까.

-

## 막힌 자리

- (없으면 "없음")

---

## 내일 미리 보기

내일은 접수 키오스크 입력 폼을 만듭니다. 택배 정책은 CJ대한통운, 화면은 CU 편의점 키오스크를 참고합니다.

각 칸의 **4번(어디서 막나)** 이 그대로 내일의 설계도입니다.

- **기계적 차단**으로 표시한 것 -> 내일 화면에서 구현한다
- **정책**으로 표시한 것 -> 선택지를 열어 두고 협의 대상으로 남긴다
- **운영**으로 표시한 것 -> 상담원 연결이나 문의 버튼으로 뺀다

키오스크를 안 써봤으면 유튜브 브이로그를 5개 봅니다. 화면이 바뀌는 단위로 따라가며 보면 대부분 해결됩니다. 5개 중 3개 이상에서 같은 것이 반복되면 그건 그렇다고 봐도 됩니다.
